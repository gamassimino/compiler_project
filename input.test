class test {
  integer algo, otro[4];

  integer metodo(){
    integer atributometodo;
    if (true || false){
      flag = true;
      flag += true;
      flag -= true;
    }
    else{
      while (true){
        return metodo.algo.algo.algo(2+2);
      }
      flag = true;
      flag += true;
      flag -= true;
    }
    this.metodo(this.metodo());
  }
}